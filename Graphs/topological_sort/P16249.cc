#include <iostream>
#include <vector>
#include <utility>
#include <stack>
using namespace std;

using VS  = vector <string>;
using VE  = vector <int>;
using VVE = vector <VE>;

int n,m;

list <int> topological_sort() {
    
}

int main() {
    while (cin >> n) {
        for (int i = 0; i < n; ++i) {

        }
        cin >> m;
        for (int i = 0; i < n; ++i) {

        }
        topological_sort();
    }
}
