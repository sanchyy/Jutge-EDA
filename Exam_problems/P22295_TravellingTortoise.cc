#include <iostream>
#include <vector>
using namespace std;

using VC  = vector <char>;
using VVC = vector <VC>;
using VE  = vector <int>;
using VVE = vector <VE>;

int n,m;
int xi,xj,yi,yj;
VVC M;



void f(int ai, int aj) {
    if ()
}

int main() {
    cin >> n >> m;
    M = VVC(n,VC(m));
    for (int i = 0; i < n; ++i)
        for (int j = 0; j < m; ++j)
            cin >> M[i][j];
    cin >> xi >> xj >> yi >> yj;
    f();
}
